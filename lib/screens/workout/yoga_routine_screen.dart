import 'package:flutter/material.dart';

class WorkoutYogaRoutineScreen extends StatelessWidget {
  const WorkoutYogaRoutineScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Yoga Routine (Workout)')),
      body: const Center(child: Text('Yoga routine placeholder (workout)')),
    );
  }
}
